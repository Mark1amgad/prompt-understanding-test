---
title: Prompt Understanding Test
emoji: 🐢
colorFrom: gray
colorTo: gray
sdk: gradio
sdk_version: 5.44.1
app_file: app.py
pinned: false
license: apache-2.0
---

Check out the configuration reference at https://huggingface.co/docs/hub/spaces-config-reference
# 🧪 مشروع رقم 6 — اختبار فهم النماذج للنصوص (Prompt Understanding Test)

## 1) إنشاء Space
1. أنشئ حسابًا على Hugging Face.
2. من تبويب **Spaces** → **Create new Space**.
3. النوع: **Gradio** — اللغة: **Python** — الرخصة: أي.
4. ارفع الملفات التالية كما هي: `app.py`, `requirements.txt`, `README.md`.

## 2) إعداد الـ API Key
1. من صفحة الـSpace → **Settings** → **Secrets**.
2. أضف سرًّا باسم: `HF_TOKEN` وقيمته الـ Access Token من حسابك (من Settings → Access Tokens → New token).
3. (اختياري) أضف متغيرًا باسم `MODEL_REPO` لتغيير الموديل الافتراضي.

> يفضّل استخدام موديل مفتوح مثل: `HuggingFaceH4/zephyr-7b-beta` أو `google/gemma-2-2b-it`.

## 3) التشغيل
- بعد رفع الملفات وضبط السرّ، سيقوم الـSpace بعملية Build تلقائيًّا.
- عند الفتح ستجد واجهة Gradio:
  - مربع للـPrompt
  - اختيار الموديل
  - إعدادات `max_new_tokens` و `temperature`
  - زر **اختبر الآن**

## 4) كيف يتم التقييم؟ (خوارزمية مبسطة)
- **تشابه الدلالة** بين الـPrompt والرد باستخدام **Sentence-Transformers** (Cosine Similarity).
- **مطابقة التنسيق** لو كان الـPrompt يطلب "قائمة" أو "كود" أو "ترجمة" أو "تلخيص".
- **ثراء الإجابة** (طول/معلومات كافية).
- الدرجة الكلية = 60% تشابه + 20% تنسيق + 20% طول.  
- الحكم النهائي: ✅ جيد (≥70) — ⚠ متوسط (50–69.9) — ❌ ضعيف (<50).

## 5) التسليم (طبقًا لملف المشروع)
أنشئ ملف PDF يحتوي على:
- رابط الـSpace (قابل للفتح من أي جهاز).
- Screenshot لواجهة التطبيق وهي تعمل.
- شرح قصير للفكرة + الأدوات + خطوات التنفيذ.

## 6) أفكار تحسين (لو وقتك يسمح)
- استبدال التقييم البسيط بنموذج مصغّر للتصنيف (Good/Bad) مدرب على أمثلة.
- إضافة **History** للاختبارات وتصدير النتائج CSV.
- دعم اختيار أكثر من موديل للمقارنة في نفس الشاشة.
- إضافة **Instruction Guidelines** للمستخدم (أمثلة Promptات).